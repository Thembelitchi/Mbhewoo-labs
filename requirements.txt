# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation and settings
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Database — Postgres ORM (async)
sqlalchemy>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0

# Graph database
neo4j>=5.20.0

# HTTP client (async)
httpx>=0.27.0

# Templating
jinja2>=3.1.4
python-multipart>=0.0.9

# Background scheduling (NOT Celery — see CLAUDE.md)
apscheduler>=3.10.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
aiosqlite>=0.20.0        # async SQLite driver — test database fixture only
ruff>=0.4.0
